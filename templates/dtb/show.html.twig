{% extends 'base.html.twig' %}

{% block title %}Dtb{% endblock %}

{% block body %}
    <h1>Dtb</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dtb.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('dtb_index') }}">back to list</a>

    <a href="{{ path('dtb_edit', {'id': dtb.id}) }}">edit</a>

    {{ include('dtb/_delete_form.html.twig') }}
{% endblock %}
